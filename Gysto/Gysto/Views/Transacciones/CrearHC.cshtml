@model 

@using (Html.BeginForm("ListadoPaciente", "Roles", FormMethod.Get))
{
    @*@Html.HiddenFor(model => model.id_turno)*@
    <div class="col-md-6">
        @Html.TextBoxFor(model => model.dni, htmlAttributes: new { @class = "form control", @id = "txtNombre" })
    </div>
    <div class="col-md-6">
        <div class="col-lg-6">
            <button class="btn btn-primary" type="submit">
                Buscar
            </button>
        </div>

    </div>
}
